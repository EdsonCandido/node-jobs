// require('@babel/register')
const path = require("path");

module.exports = {
  "config": path.resolve(__dirname, "dist", "config", "database.js"),
  "modules-path": path.resolve(__dirname, "dist", "model"),
  "migrations-path": path.resolve(__dirname, "dist", "database", "migrations"),
  "seeders-path": path.resolve(__dirname, "dist", "database", "seeds")
};
